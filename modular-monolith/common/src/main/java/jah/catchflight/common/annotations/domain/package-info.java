/**
 * Contains annotations used to mark classes and interfaces related to domain-driven design (DDD)
 * concepts in the CatchFlight application.
 * <p>
 * This package provides annotations such as {@code @DomainAggregate}, {@code @DomainFactory},
 * {@code @DomainPolicy}, {@code @DomainRepository}, {@code @DomainService}, and
 * {@code @DomainValueObject} to clearly define the roles of domain components, promoting a
 * consistent and structured approach to modeling the business domain.
 * </p>
 *
 * @since 1.0
 */
package jah.catchflight.common.annotations.domain;
